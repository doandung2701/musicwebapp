var initialState={
    loading:false,
    currentUser:null,
    authenticated:false,
    loginfalse:null,
    error:false
}
export const authenReducer=(state=initialState,action)=>{
    switch (action.type) {
        default:
        return state
    }
}